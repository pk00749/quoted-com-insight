# 应用运行时可配置参数
# 公告时间范围（单位：天）
announcement_time_range_days: 1

# PDF正文截断长度（单位：字符/字）
pdf_content_max_chars: 500

# 订阅刷新定时执行时间（北京时间 HH:MM）
subscription_refresh_time: "09:00"

# 日志配置（默认写入容器内 /app/logs/app.log，可通过 LOG_PATH 覆盖）
log_level: "INFO"
log_path: "/app/logs/app.log"
